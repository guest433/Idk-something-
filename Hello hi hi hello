local Library = loadstring(game:HttpGet("https://pastebin.com/raw/vff1bQ9F"))() 
-- can be any gui but you have to replace the bottons and text you can create a function but I don't think it's gonna work i did it but it doesn't work go search in YouTube make sure to delete this idk if this would work you can see it's a lot of tex

local Window = Library.CreateLib("key system", "Synapse") 
-- can be any theme you want

local Key = Window:NewTab("key system") 

local KeySection = Key:NewSection("Key") 

local key = "keyhereorthelink"


local key2
KeySection:NewTextBox("key", "enter key", function(enter) 

	key2 = enter
    -- put whatever you want

end)

KeySection:NewButton("check key", "checks your key", function() 
-- this button will check key

    if key2 == key then
      local Library = loadstring(game:HttpGet("https://pastebin.com/raw/vff1bQ9F"))()
local Window = Library.CreateLib("key system", "Synapse")

local Key = Window:NewTab("Key")
local KeySection = Key:NewSection("Key")

local key = "key"
local key2

KeySection:NewTextBox("key", "enter key", function(enter)
    key2 = enter
end)

KeySection:NewButton("check key", "checks your key", function()
    if key2 == key then
        loadstring(game:HttpGet("https://raw.githubusercontent.com/guest433/Something-don-t-click-k/main/Hello"))()
        for i, v in pairs(game.Players.LocalPlayer.PlayerGui:GetChildren()) do
        if v:IsA("ScreenGui") and v.Name == "key system" then
            v:Destroy()
        end
    end
end)
      print("correctkey")
    else
      print("Wrongkey")
    end
end)




local thekey= Window:NewTab("the key")
local thekeySection = thekey:NewSection("copy key here")


thekeySection:NewButton("Key", "", function()
    setclipboard("keyhereorthelink")
end)
